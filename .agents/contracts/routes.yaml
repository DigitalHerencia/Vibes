id: vibes.routes
version: 1
authority: current-source-contract
public: ["/", "/pricing", "/faq"]
auth: ["/sign-in", "/sign-up"]
protected: ["/dashboard", "/projects", "/projects/new", "/projects/[projectId]", "/settings"]
api: ["/api/clerk/webhooks", "/api/stripe/webhooks"]
reference_catalog:
  status: currently-exposed
  route_group: app/(presentation)
  known_gap: reference catalog is not yet isolated from default production routes
