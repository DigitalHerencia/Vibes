id: vibes.domain-model
version: 1
authority: current-source-contract
current_models:
  - User
  - Project
  - ProjectMembership
  - AuditEvent
  - ProviderWebhookEvent
identity:
  provider: clerk
  local_model: User
authorization:
  current_scope: project rows
  membership_model: ProjectMembership
tenant:
  abstraction_required: true
  selected_name: null
  project_is_tenant: false
known_gaps:
  - tenant model and tenant-scoped RLS are not implemented
  - capability-based RBAC is not implemented
